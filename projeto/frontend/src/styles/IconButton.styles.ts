import styled from 'styled-components';

const IconButton = styled.button`
  align-items: center;
  padding: 0.3em;
  background: none;
  border: none;
  cursor: pointer;
`;

export default IconButton;
